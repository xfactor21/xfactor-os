name: Design Lab Brand + Pixel Utilities 2.0 Acceptance

on:
  pull_request:
    paths:
      - 'src/modules/studio/tools/BackgroundRemover.tsx'
      - 'src/modules/studio/tools/LogoMaker.tsx'
      - 'src/modules/studio/tools/PixelArt.tsx'
      - 'scripts/design-brand-pixel-smoke.mjs'
      - '.github/workflows/design-brand-pixel-2-acceptance.yml'
  workflow_dispatch:

jobs:
  brand-pixel:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - name: Install Chromium
        run: |
          npm install --no-save --package-lock=false --legacy-peer-deps playwright@1.62.0
          npx playwright install --with-deps chromium
      - name: Start production preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > /tmp/xfactor-brand-pixel-preview.log 2>&1 &
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/xfactor-brand-pixel-preview.log
          exit 1
      - name: Brand + pixel browser acceptance
        run: node scripts/design-brand-pixel-smoke.mjs