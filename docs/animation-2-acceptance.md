# Animation 2.0 Acceptance

Required before merge:

- clean install succeeds
- lint passes
- production build passes
- Animation 2.0 opens from Design Lab without page errors
- adding a shape persists a real animation object
- key-all creates per-property keyframes at the active frame
- duration and work-area controls persist exact values
- duplicate, visibility and lock controls mutate persistent object state
- selected objects expose per-property timeline tracks
- animation document survives reload without render failure
- full release-candidate verification remains green
